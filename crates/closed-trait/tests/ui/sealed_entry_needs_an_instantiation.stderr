error: `Plain` does not say which `Store` it implements, so nothing can check that it implements `Store` at all.
       Write `Plain: Store<..>` with the arguments it implements
 --> tests/ui/sealed_entry_needs_an_instantiation.rs:8:10
  |
8 | #[sealed(Plain, for<T> Boxed<T>: Store<T>)]
  |          ^^^^^
