error: `Plain` does not name `T`, which the generated enum is generic over, so there is no single `AnyStore` it can turn into.
       Say how it instantiates the trait: `Plain: Store<..>`
 --> tests/ui/enumerate_entry_misses_parameter.rs:7:10
  |
7 | #[sealed(Plain, Boxed<T>)]
  |          ^^^^^

error: `Plain` does not say which `Store` it implements: it names neither `T` nor an instantiation, so nothing can check that it implements `Store` at all.
       Write `Plain: Store<..>` with the arguments it implements, or name the trait's parameters in the type itself
 --> tests/ui/enumerate_entry_misses_parameter.rs:7:10
  |
7 | #[sealed(Plain, Boxed<T>)]
  |          ^^^^^
